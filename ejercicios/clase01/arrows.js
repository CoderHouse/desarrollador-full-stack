const funcionArrow = (dato) => {
    return dato.toUpperCase();
};

const funcionArrow = dato => {
    return dato.toUpperCase();
};

const funcionArrow = dato => {
    return dato.toUpperCase()
};

const funcionArrow = dato => dato.toUpperCase();


console.log(funcionArrow('texto en mayusculas'));
